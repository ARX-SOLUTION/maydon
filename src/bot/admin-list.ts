/**
 * Admin listing & removal — /admins, /removeadmin (owner-only).
 * Ticket #12 fills this in.
 */
import { bot } from "./client.ts";

export function registerAdminList(): void {
  // ponytail: intentional stub — implemented by ticket #12.
  void bot;
}

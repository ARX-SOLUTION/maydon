/**
 * User moderation — block/unblock via forwarded message, /blocked (owner-only).
 * Ticket #13 fills this in.
 */
import { bot } from "./client.ts";

export function registerModeration(): void {
  // ponytail: intentional stub — implemented by ticket #13.
  void bot;
}

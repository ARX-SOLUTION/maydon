/**
 * Bot command menu (setMyCommands, scoped user vs admin), /help, /cancel.
 * Ticket #15 fills this in — depends on the final command set from #10/#11/#12/#13.
 */
import { bot } from "./client.ts";

export function registerCommands(): void {
  // ponytail: intentional stub — implemented by ticket #15.
  void bot;
}
